handoffs = {
    1: {
        "id": 1,
        "current_owner": "party1",
        "initiated_by": "party1",
        "receiving_party": None,
        "state": "active",
        "events": []
    }

    # active -> pending (initiate)
    # pending -> accepted (accept)
    # pending -> active (decline)
}